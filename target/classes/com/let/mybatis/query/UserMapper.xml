<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.let.mybatis.mapper.UserMapper">
    
    <select id="dbtest" parameterType="DataMap" resultType="DataMap">
    	SELECT
    		content
    	FROM
    		test
    	WHERE
    		id=#{id}
    </select>
    

	
</mapper>